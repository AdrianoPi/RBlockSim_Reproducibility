#pragma once

#include <ROOT-Sim.h>

typedef uint32_t node_id_t;
#define NODE_ID_MAX UINT32_MAX
typedef uint32_t block_id_t;
typedef uint32_t block_seq_id_t; ///< Miner-level sequential ID for Blocks, unique in each miner
typedef unsigned txn_id_t;
